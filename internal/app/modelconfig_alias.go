package app

import "feidex/internal/app/modelconfig"

// Constant aliases — model config defaults
const modelConfigDefaultOptionValue = modelconfig.DefaultOptionValue

// Function aliases — model lookup helpers
var (
	configuredGlobalModel = modelconfig.ConfiguredGlobalModel
	modelCardActionRow    = modelconfig.ModelCardActionRow
)
